# CLAUDE.md – Notizblock App

> Projektkontext für Claude Code. Kurz halten – wird in jeder Session komplett geladen.
> Wenn Claude etwas falsch macht und ich es korrigiere: hier eine Regel ergänzen, damit der Fehler nicht wiederkommt.

## Was das ist

Plattformübergreifende Notizblock-App in **Flutter (Dart)**.
Eine Codebasis für **Android, Windows und Linux**.

Kernfunktionen:
- Reine Text-Notizen (kein Markdown, keine Bilder, keine Checklisten)
- Post-it-Style Widgets: auf Android Homescreen-Widgets, auf Windows „Sticky Notes"-Fenster
- Schlichtes Design, abgerundete Ecken
- Mehrsprachig, **Deutsch als Standard**, Sprache umschaltbar
- Google-Drive-Backup/Sync für plattformübergreifenden Abgleich

## Tech-Stack

- **State:** provider
- **Lokale DB:** sqflite (+ sqflite_common_ffi für Windows/Linux), path, path_provider
- **Google Drive:** googleapis, googleapis_auth, google_sign_in, http
- **Widgets (Android):** home_widget
- **Desktop-Fenster:** window_manager (Position/Größe der Sticky-Notes), url_launcher (OAuth-Browser)
- **UI:** flutter_staggered_grid_view, Material Design
- **Sonstiges:** uuid, intl, shared_preferences, flutter_localizations

## Architektur

```
lib/
  models/      # Note-Datenklasse
  providers/   # State (provider)
  screens/     # Bildschirme/Seiten
  widgets/     # wiederverwendbare UI-Komponenten
  services/    # database_service.dart, google_drive_service.dart, …
  l10n/        # Lokalisierung (de = Standard)
```

**Note-Model:** id (uuid), title, content, createdAt, modifiedAt, color (Default `#FFFDE7`), isPinned, isArchived.
**DatabaseService:** Singleton; auf Windows/Linux `sqfliteFfiInit()` + `databaseFactoryFfi`; Tabelle `notes` mit Indizes auf `modifiedAt` und `isPinned`. DB-Version aktuell 1 → Schema-Änderungen über `_onUpgrade` migrieren, nicht hart neu anlegen.

## App-Icon

Design: **blaue Kachel (`#1E88E5`) + gelber Notizblock (`#FFEB3B`) + roter Stift**. Quelle: `assets/icon/icon_full.png` (Voll-Icon) + `assets/icon/icon_foreground.png` (transparenter Vordergrund für Android-Adaptive); beide via PowerShell/GDI+ erzeugt. Eingebunden via `flutter_launcher_icons` (Config in `pubspec.yaml`): Android (legacy mipmaps + adaptive, Hintergrund `#1E88E5`). Für **Windows** wird zusätzlich eine **Multi-Size-`.ico`** (16–256 px) per Skript nach `windows/runner/resources/app_icon.ico` geschrieben (flutter_launcher_icons macht nur eine Größe → kleine Icons sonst leer/„weißer Zettel").
- **Stolpersteine beim Icon:** (1) CMake verfolgt `app_icon.ico` nicht zuverlässig → nach Icon-Wechsel ggf. `flutter clean` vor dem Windows-Build, sonst bleibt das alte Icon eingebettet. Eingebettetes Icon prüfen via `[System.Drawing.Icon]::ExtractAssociatedIcon(exe)`. (2) Windows cached Icons → nach Wechsel Icon-Cache leeren (`IconCache.db` + `Explorer\iconcache_*.db` löschen, Explorer neu starten), sonst zeigt die Verknüpfung weiter das alte/leere Icon.

## Befehle

```powershell
flutter pub get              # Abhängigkeiten
flutter analyze              # Statische Analyse – vor jedem Commit
flutter run -d windows       # Lokal testen (Windows)
flutter build apk --release  # Android
flutter build windows --release

# Windows-Installer (Setup.exe) bauen – baut Release + bündelt VC++-Runtime,
# kompiliert via Inno Setup nach installer\output\Notizblock-Setup-<ver>.exe:
powershell -ExecutionPolicy Bypass -File installer\build_installer.ps1
#   -SkipBuild      # nutzt vorhandenen Release-Build (kein Neu-Build)
#   -Version 1.2.0  # Versionsnummer für diesen Lauf überschreiben
```

Bei verkeiltem `flutter clean` (Datei in Verwendung): laufende `dart.exe`/App schließen, dann erneut.

## Konventionen

- Code-Kommentare und UI-Texte auf **Deutsch**.
- UI-Strings nicht hartkodieren → über l10n (de zuerst), damit Mehrsprachigkeit nicht bricht.
- Plattformabhängigen Code (Desktop vs. Android) sauber über `Platform.is…` bzw. bedingte Importe kapseln.
- Antworten/Erklärungen an mich: technisch präzise und knapp, kein Füllwerk. Bei Unsinn direkt widersprechen.
- Vor größeren Änderungen kurz den Plan nennen, nicht blind über bestehenden Code bügeln.

## Bekannte Stolpersteine / offene Punkte

- **home_widget:** mind. `^0.7.0` – ältere Versionen ziehen das gelöschte Android-**v1-Embedding** und der Build scheitert. Bei „deleted Android v1 embedding"-Fehler zuerst hier ansetzen.
- **sqlite3.dll auf Windows/Linux:** `sqflite_common_ffi` lädt die native `sqlite3`-Lib zur Laufzeit, bündelt sie aber nicht selbst. Ohne `sqlite3_flutter_libs` als Dependency wirft `openDatabase()` „Failed to load dynamic library 'sqlite3.dll'" → quasi die ganze Desktop-App tot. `sqlite3_flutter_libs` ist daher Pflicht (steht in `pubspec.yaml`).
- **Windows-Build braucht VS-C++-Workload:** `flutter doctor` muss „Desktop development with C++" grün zeigen (MSVC, C++ CMake tools, Windows SDK). Fehlt die Workload → „Unable to find suitable Visual Studio toolchain". Im Visual-Studio-Installer nachinstallieren.
- **Linux (Zorin OS):** Plattform via `flutter create --platforms=linux .` ergänzt (`linux/`-Runner, Binary `notizblock`, App-ID `at.aw.notizblock`). **Build nur auf Linux-Host** (nicht von Windows aus cross-baubar). Build-Deps auf Zorin/Ubuntu: `clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libstdc++-12-dev`. Bauen + installieren über `scripts/install_linux.sh` (legt Bundle nach `~/.local/opt/notizblock`, Icon nach hicolor, `.desktop`-Launcher). Plattform-Anpassungen: Autostart = `.desktop` in `~/.config/autostart/` (`AutostartService`, Linux-Zweig); Sticky-Prozess-Check via `/proc/<pid>` + Schließen via `kill` (statt `tasklist`/`taskkill`); UI-Gates auf `_isDesktop` statt `Platform.isWindows`. Fenster-Icon: `gtk_window_set_icon_name(window,"notizblock")` in `my_application.cc` → löst sich nach Icon-Install auf. **Wayland-Caveat:** absolute Fensterposition (`window_manager` setPosition/getBounds für Sticky-Bounds + `--pos`) funktioniert nur unter **X11** zuverlässig; unter Wayland ignoriert der Compositor App-Positionen → ggf. Xorg-Session wählen.
- **google_sign_in hat keine Desktop-Impl:** `signInSilently()`/`signIn()` daher nur auf Android/iOS aufrufen (in `main.dart` per `Platform.is…` gekapselt), sonst Exception beim Start.
- **Google-Drive-OAuth – Desktop läuft & getestet, Android offen.** Auth-Schicht ist plattformgeteilt: Desktop (Windows/Linux/macOS) nutzt `googleapis_auth` `clientViaUserConsent` (Loopback-Server + Browser via `url_launcher`), Token werden in `shared_preferences` (`drive_desktop_credentials`) persistiert → stiller Re-Login. Mobil (Android/iOS) nutzt weiter `google_sign_in`. Desktop-`client_id`/`client_secret` (Typ „Desktop app") stehen in `lib/services/google_drive_config.dart` (enthält echte Secrets → bei Git unbedingt gitignoren). Cloud-Projekt-ID `445597891658`, Drive API aktiviert, Test-User eingetragen. Sync = last-write-wins über `modifiedAt` (so gewollt). **Offen für Android:** OAuth-Client Typ „Android" mit Paketname + SHA-1 in der Cloud Console anlegen, dann auf echtem Gerät testen.
- **Android-Homescreen-Widget (`home_widget` + `NoteWidgetProvider.kt`):** Liest die aktuelle Notiz aus `app_flutter/notes.json` (von `DatabaseService._updateWidgetJsonFile` geschrieben). Layout `note_widget.xml`: Content-`maxLines` hoch (1000) → zeigt beim Vergrößern mehr. Notizfarbe wird im Provider auf den Hintergrund übernommen (API≥31 `setBackgroundTintList` → Ecken bleiben rund, sonst `setBackgroundColor`), Textfarbe nach Luminanz. **WICHTIG:** Update-Broadcast muss voll qualifiziert sein (`qualifiedAndroidName='com.example.notizblock.NoteWidgetProvider'` in `WidgetService.updateWidget`), weil die Provider-Klasse im Namespace `com.example.notizblock` liegt, die applicationId aber `at.aw.notizblock` ist – sonst trifft der Refresh die Klasse nicht. **WICHTIG – Broadcast zentral in `_updateWidgetJsonFile`:** Nach dem Schreiben von `notes.json` ruft `DatabaseService._updateWidgetJsonFile` direkt `WidgetService.instance.updateWidget()` auf. Das ist der gemeinsame Chokepoint ALLER DB-Schreibwege (auch Delta-Sync/`insertOrUpdateNotes`). Vorher feuerte der Broadcast nur über `NotesProvider` bei lokalen Aktionen → Remote-Sync-Änderungen aktualisierten das Widget erst nach dem 1-h-`updatePeriodMillis`. Den Broadcast also NICHT aus `_updateWidgetJsonFile` entfernen.
- **Editor speichert beim Tippen (entprellt 1 s) bei bestehenden Notizen** (`_saveDebounce` in `NoteEditorScreen`), damit der Auto-Sync zeitnah hochlädt statt erst beim Schließen. Neue Notizen weiterhin erst beim Schließen (sonst Duplikate, da `widget.note` null bleibt). Lifecycle: Sync auch bei `paused` (App geht in den Hintergrund) – nicht nur `resumed` –, damit Änderungen vor dem Einfrieren der Timer noch hochgehen.
- **Cross-Device-Pull (alle Plattformen):** `NotesProvider._remotePullTimer` zieht alle 15 s von Drive (`_runAutoSync`). Läuft nur im Vordergrund – auf Android pausieren Timer im Hintergrund (kein Akkuverbrauch). Zusätzlich: einmaliger Sync ~2 s nach Start **und** Sofort-Sync bei Rückkehr in den Vordergrund (`WidgetsBindingObserver.didChangeAppLifecycleState` → resumed). Alles nur bei aktivem Auto-Sync + Anmeldung. So fließt es in beide Richtungen (PC↔Handy).
- **Hintergrund-Sync Android (`workmanager`, `BackgroundSyncService`):** Da Vordergrund-Timer im Hintergrund pausieren, zieht zusätzlich ein **WorkManager-Periodic-Job** alle **1 h** auch bei geschlossener App von Drive – so aktualisiert sich das Homescreen-Widget mit Fremdgeräte-Änderungen ohne App-Öffnen. Top-Level `backgroundSyncCallbackDispatcher` (`@pragma('vm:entry-point')`, eigenes Isolate): `ensureInitialized` → Auto-Sync-Pref prüfen → `signInSilently()` → `synchronize()`; der Widget-Broadcast läuft dann über `DatabaseService._updateWidgetJsonFile`. Job-Lifecycle an den Auto-Sync-Schalter gekoppelt (`SettingsProvider.setAutoSync` ruft `enable()`/`disable()`; `main()` re-registriert bei Start, wenn Pref an). Constraints: `NetworkType.connected` = **beliebiges Netz inkl. mobiler Daten** (bewusst nicht nur WLAN), `ExistingPeriodicWorkPolicy.keep`, `initialDelay` 15 min. **Kein Custom-Application-Setup nötig** – workmanager 0.9.x registriert sich über `androidx.startup`; Permissions (INTERNET/ACCESS_NETWORK_STATE/WAKE_LOCK/RECEIVE_BOOT_COMPLETED) werden automatisch gemergt. Android-Minimum für Periodic ist 15 min; 1 h ist akkuschonend (~0,3–1 %/Tag), reale Intervalle dehnt Doze. Nur Android (iOS/Desktop = no-op).
- **Sticky-Fenster syncen selbst (Windows):** Jedes Sticky-Fenster synct automatisch – nach Änderung (entprellt 2 s) + periodischer Pull (15 s), gegated über den Auto-Sync-Schalter (`_runStickyAutoSync` in `StickyNoteScreen`). Der kleine Sync-Button unten rechts (`_syncNow`) ist nur Absicherung. Dafür meldet sich der Sticky-Prozess in `main()` per `signInSilently` an. Mehrere Prozesse syncen ggf. gleichzeitig → `synchronize()` macht `prefs.reload()` für prozessübergreifend frische Zeitstempel.
- **Android-Widget-Navigation & Buttons:** Widget-Tap → `_openNoteEditor` öffnet den Editor via `pushAndRemoveUntil` als EINZIGEN Screen → einmal Zurück führt zum Homescreen (`SystemNavigator.pop` wenn `!canPop`). `NoteEditorScreen.openedFromWidget` zeigt dann oben einen Home-Button (`_goToMainMenu` → `HomeScreen` als neue Wurzel). Das Android-Homescreen-Widget hat zusätzlich ein ☰-Symbol (`widget_menu`), das die App via `ACTION_MAIN`+`CLEAR_TASK` frisch in der Hauptansicht öffnet. Editor hat außerdem einen manuellen Sync-Button (Absicherung).
- **Auto-Sync:** Schalter „Automatisch synchronisieren". `NotesProvider._scheduleAutoSync()` löst nach jeder Datenänderung einen **entprellten** Sync aus (2 s nach der letzten Änderung), wenn angemeldet + Schalter an – **auf allen Plattformen** (Android lädt dank Delta nur die geänderte Notiz hoch). Nur das Fremdänderungs-Polling (`_externalChangeTimer`, 0,8 s) bleibt **Desktop-exklusiv**.
- **Delta-Sync / Drive-Layout (seit Umstieg von Single-File):** Im Ordner „Notizblock Backup": `notes/<id>.json` (aktueller Stand je Notiz, inkl. archivierte mit `isArchived`-Flag), `tombstones/<id>.json` (`{id, deletedAt}` für Löschungen), `history/<id>__<ts>__<titel>.json` (Versions-Snapshots). `notizblock_backup.json` bleibt als Voll-Backup-Fallback (nur von „Backup erstellen" geschrieben, NICHT vom Delta-Sync). `synchronize()` in `google_drive_service.dart`: Pull nur Dateien mit `modifiedTime > lastRemoteSync` (Drive-Zeit, in prefs `last_remote_sync_time`), Push nur lokal seit `last_sync_timestamp` geänderte Notizen + offene Tombstones (DB-Tabelle `deletions`, via `_afterLocalWrite`/`getPendingDeletions`). Last-write-wins über `modifiedAt` vs. `deletedAt`. Reentrancy: `synchronize()` hält den laufenden Sync als `_inFlight`-Future; ein paralleler Aufruf (z.B. Button während Auto-Sync) bekommt denselben Future zurück (kein „läuft bereits"-Fehler mehr). Eigentliche Logik in `_doSynchronize()`. Historie: pro Notiz neueste **30** behalten; gelöschte Notizen (Tombstone + Historie) nach **30 Tagen** entsorgt (`_pruneHistory`, läuft nur wenn etwas synchronisiert wurde). Einmalige Migration vom alten Gesamt-Backup über `_migrateLegacyIfNeeded` (prefs `delta_migrated`), respektiert `modifiedAt` (überschreibt neuere lokale Notizen nicht). DB-Version **2** (`deletions`-Tabelle, `_onUpgrade`). Löschungen propagieren jetzt korrekt (vorher Bug: gelöschte Notizen kamen via Big-File zurück).
- **Prozessübergreifende Aktualisierung (Desktop):** Sticky-Note-Fenster sind eigene Prozesse und schreiben direkt in die DB. Die Hauptapp pollt deshalb alle 1,5 s `DatabaseService.getLastModified()` (mtime von `.db`/`-wal`/`-journal`/`-shm`). Bei Fremdänderung: stilles Neuladen der Liste **und** Auto-Sync zu Drive. So ist die Hauptapp die einzige Sync-Instanz (keine konkurrierenden Uploads aus mehreren Prozessen). `_lastDbMtime` wird nach eigenen Schreibvorgängen via `_afterLocalWrite()` gesetzt, damit der eigene Write nicht als Fremdänderung gilt. Voraussetzung fürs Sync von Widget-Edits: Hauptprozess läuft (tut er normal, beim Autostart minimiert).
- **Live-Editor (`NoteEditorScreen`):** hört auf `NotesProvider` und übernimmt externe Änderungen an der offenen Notiz live in Titel/Inhalt – aber nur, wenn das jeweilige Feld **nicht fokussiert** ist (`FocusNode.hasFocus`), damit aktuelles Tippen nie überschrieben wird. So ziehen mehrere offene Fenster derselben Notiz nach.
- **Sticky-Fenster live (`StickyNoteScreen`):** eigener Prozess → pollt selbst alle 1 s `getNoteById` und übernimmt externe Änderungen (Inhalt nur wenn Feld nicht fokussiert; Farbe/Titel immer). Extern gelöschte Notiz → Fenster schließt sich. Nötig, weil ein Sticky-Fenster sonst seinen Stand vom Öffnungszeitpunkt behält.
- **WICHTIG – `modifiedAt` nie ohne echte Änderung bumpen:** `Note.copyWith` setzt `modifiedAt = now`. Auto-Sync-Pfade dürfen `_saveNote` daher NUR aufrufen, wenn sich Titel/Inhalt wirklich geändert haben (Sticky `_saveNote` vergleicht vorher; Editor speichert beim Sync nur bei `_hasChanges`; externe Übernahmen setzen via `_applyingExternal` kein `_hasChanges`). Sonst bumpte jedes offene Fenster alle 30 s den Zeitstempel auf „jetzt" → Anzeige zeigt immer die aktuelle Zeit UND das Fenster gewann fälschlich last-write-wins → Änderungen anderer Geräte gingen verloren. (War real ein Bug.)
- **Änderungszeit-Anzeige:** Notizkarte (Liste) zeigt `modifiedAt`; Windows-Sticky unten links (`DateFormat('d.M. HH:mm')`); Android-Widget rechts vom Titel (`widget_time`, Provider `formatTime` per Substring, kein java.time wegen Desugaring).
- **WICHTIG – `last_sync_timestamp`-Watermark = Sync-START, nicht Sync-Ende:** `_doSynchronize()` hält das neue lokale Watermark **vor** dem Erfassen der geänderten Notizen fest (`localSyncStart = DateTime.now()`) und schreibt am Ende genau diesen Wert – NICHT `DateTime.now()` am Ende. Sonst: eine Notiz, die WÄHREND eines laufenden Syncs gespeichert wird (z.B. weiter getippt, nachdem ihr alter Stand schon gepusht war), hat ein `modifiedAt` < End-`now()` → der nächste `getNotesModifiedAfter()`-Lauf überspringt sie → sie wird NIE hochgeladen. War real ein Bug: nur Zwischenstand „Test m" landete in Drive, finaler Stand „Test machen" wurde verschluckt. Trat fast nur am Handy auf (Tippen mit Pausen ⇒ ständig Save-(1 s)+Auto-Sync-(2 s)-Zyklen mitten im Editieren + Sync bei `paused`; langsameres Mobilnetz = breiteres Race-Fenster). Watermark also NICHT wieder auf End-`now()` ändern.
- **Sync-Konflikt = last-write-wins über `modifiedAt` (Gerät-Uhr!).** Bei Uhren-Differenz zwischen Geräten kann theoretisch die „falsche" Version gewinnen. **Safety-Net:** jede gepushte Version liegt vorher als Snapshot in `history/`, also nichts ist je wirklich verloren – im Zweifel von dort wiederherstellen. `synchronize()` überspringt beim Push IDs, die im selben Lauf gerade per Pull übernommen wurden (`appliedIds`) → verhindert Ping-Pong/Zurückschreiben.
- **Drive-Setup-Stolpersteine (real aufgetreten):** „403 access_denied" = Test-User fehlt im OAuth-Consent-Screen. „403 Drive API has not been used/disabled" = Drive API im richtigen Projekt aktivieren (leicht im falschen Projekt zu aktivieren!). Beides reine Cloud-Config, kein Code.
- **Windows-Widgets / Sticky Notes:** Umgesetzt über **separate Prozesse** (`Process.start … --sticky-note <id>`), NICHT `desktop_multi_window`. Jeder Prozess steuert sein Fenster via `window_manager`. „Widget"-Status (welche Notizen beim Start aufgehen) = `ids`-Liste in eigener Datei `<AppSupport>/sticky_state/widget_ids.json` (`StickyNoteService.getWidgetNoteIds`/`setWidget`), pro Notiz per Rechtsklick-Kontextmenü umschaltbar. **WICHTIG – NICHT in shared_preferences:** Lag früher dort mit der falschen Annahme „schreibt nur die Hauptapp → unkritisch". Real schreiben aber auch die Sticky-Prozesse in dieselbe prefs-Datei (beim Sync `last_sync_timestamp` etc.); da prefs immer die GANZE Datei schreibt, kippte ein Sticky-Prozess mit veraltetem Snapshot die Anheft-Änderungen der Hauptapp zurück → gelöste Widgets kamen wieder, neu angeheftete verschwanden nach dem Neustart (war real ein Bug; Workaround war: einstellen, alle Sticky-Tasks beenden, neu starten). Eigene Datei = nur die Hauptapp schreibt → behoben. Einmalige Migration vom alten prefs-Key beim ersten `getWidgetNoteIds`. `openAllWidgets()` läuft bei **jedem** Desktop-Start (nicht nur Autostart), damit angeheftete Widgets immer erscheinen; Doppel-Fenster werden über die in der Zustandsdatei gespeicherte, noch lebende PID (`tasklist`-Check) vermieden. Android nutzt weiter `home_widget`. Zum Hauptfenster-Verhalten siehe „Desktop-Startverhalten". **Neu angeheftete Widgets** öffnen auf dem Monitor des Hauptfensters: `openStickyNote` gibt dessen Position als `--pos <x> <y>` mit (window_manager-Koordinaten), das Sticky nutzt sie als Startposition, falls noch keine Bounds gespeichert sind (`closeStickyNote` schließt umgekehrt das Fenster beim „Widget lösen" per `taskkill /PID`). Schriftgröße der Notiztexte: `SettingsProvider.noteFontScaleKey` (0,8–1,8), Editor liest per `context.watch`, Sticky liest die pref direkt (pollt sie im 1-s-Takt für Live-Update). Notiz-Textfarbe wird überall per `estimateBrightnessForColor` an die (teils kräftige) Notizfarbe angepasst.
- **WICHTIG – kein shared_preferences für prozessübergreifende Writes:** SharedPreferences schreibt immer die ganze Datei; mehrere gleichzeitig laufende Sticky-Prozesse überschreiben sich dabei gegenseitig (führte real zu verlorenen PID/Bounds-Einträgen → Doppel-Fenster). Deshalb liegen **Fenster-Bounds + offen-PID in EINER Datei pro Notiz** (`<AppSupport>/sticky_state/sticky_<id>.json`, `StickyNoteService._readState/_writeState`) – pro Datei nur ein Schreiber (das Sticky-Fenster der Notiz). **Vorsicht – auch „nur von der Hauptapp geschriebene" Werte sind NICHT sicher in shared_preferences:** das Problem ist nicht, wer den Wert schreibt, sondern dass IRGENDEIN Prozess prefs schreibt und dabei seinen veralteten Gesamt-Snapshot (ALLE keys) zurückkippt. Die Sticky-Prozesse schreiben prefs beim Sync (`last_sync_timestamp` etc.), also überschreiben sie dabei auch Hauptapp-only-Keys. Deshalb gehören prozessübergreifend gelesene/relevante Werte in eigene Dateien (so jetzt auch `widget_ids.json`, s.o.). In shared_preferences nur Werte, die KEIN Sticky-Prozess je liest und deren Verlust egal ist.
- **Autostart (Windows):** `AutostartService` legt eine **Verknüpfung im Autostart-Ordner** an (`%APPDATA%\…\Startup\Notizblock.lnk`, Ziel `"<exe>" --widgets`, via WScript.Shell) – NICHT mehr den HKCU-Run-Key (wurde auf Andis System ignoriert; alter Run-Eintrag wird beim Umschalten aufgeräumt). Toggle in den Einstellungen (Windows-Sektion). Installierte App liegt unter `C:\Users\awurz\Notizblock\` (bewusst nicht in AppData – aus der Sandbox wird AppData in den Claude-Container umgeleitet).
- **Windows-Installer (`installer/`):** Inno-Setup-basiert. `installer/notizblock.iss` = Setup-Skript (per-User ohne Adminrechte → `%LocalAppData%\Programs\Notizblock`, optional Desktop-Icon + Autostart-Task, Startmenü, Deinstaller; Version per `/DMyAppVersion=` überschreibbar, `MyAppId`-GUID NICHT ändern → sonst erkennen Updates die Altinstallation nicht). `installer/build_installer.ps1` macht alles in einem Rutsch: `flutter build windows --release` → Staging (Release-Build + die 3 **VC++-Runtime-DLLs** msvcp140/vcruntime140/vcruntime140_1, damit die App auch auf frischen PCs ohne Redist läuft) → ISCC kompiliert → `installer/output/Notizblock-Setup-<ver>.exe` (~12 MB, eine Datei, auf Ziel-PC ausführen). ISCC-Pfad/VC++-Redist-Ordner werden dynamisch gesucht (keine festen Versionsnummern). `installer/stage/` + `installer/output/` sind gitignored. Inno Setup via `winget install --id JRSoftware.InnoSetup -e` (liegt unter `%LocalAppData%\Programs\Inno Setup 6\ISCC.exe`). **Nicht code-signiert** → SmartScreen-Warnung beim ersten Start ist normal. Auf neuem PC: für Drive-Sync einmal in den Einstellungen neu anmelden (OAuth), Tokens werden nicht mitinstalliert.
- **Desktop-Startverhalten (`main()`):** Bei JEDEM Desktop-Start werden die angehefteten Widgets geöffnet. **Standardmäßig erscheint KEIN Hauptfenster** – der Hauptprozess beendet sich nach dem Spawnen der (detached) Widget-Fenster mit `exit(0)`. Das Hauptfenster erscheint nur, wenn: `--show-main` (Home-Button eines Widgets startet die App so), Einstellung „Hauptfenster beim Start öffnen" aktiv (`SettingsProvider.showMainWindowOnStartKey`, Default false), **keine** Widgets angeheftet, ODER manuell gestartet und alle Widgets liefen bereits (`openedCount==0`, sonst liefe der Icon-Klick ins Leere). `--widgets` (Autostart) minimiert NICHT mehr, sondern fließt nur in die `isAutostart`-Bedingung ein.
- **Sync-Strategie:** noch festzulegen, wie Konflikte aufgelöst werden (last-write-wins über `modifiedAt`? echtes Merge?). Vor Implementierung kurz mit mir klären.

## Anpassen / prüfen ‹von Andi›

- **applicationId / Package-Name:** **`at.aw.notizblock`** (finale Play-Store-Identität, in `android/app/build.gradle.kts`). Interner Kotlin-Namespace bleibt bewusst `com.example.notizblock` (die 3 Klassen unter `kotlin/com/example/notizblock/`) – für OAuth/Store zählt nur die applicationId.
- **Android Google-OAuth:** Cloud-Projekt `445597891658`, OAuth-Client Typ „Android" mit Paketname `at.aw.notizblock` + Debug-SHA-1 `61:7D:C8:19:42:12:32:FC:71:B4:48:16:26:B7:BD:BC:FE:D5:F0:60`. `google_sign_in` matcht über Paketname+SHA-1, KEIN client_id/secret in der App nötig. **Für Release/Play Store:** zusätzlich den Release-/Play-App-Signing-SHA-1 zum selben OAuth-Client hinzufügen.
- Pfad-Hinweis: Projekt liegt verschachtelt unter `…\notizblock_app\notizblock_app` (innerer Ordner mit `pubspec.yaml` ist das Projektroot – die `CLAUDE.md` gehört dorthin).
